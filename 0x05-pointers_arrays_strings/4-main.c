#include "main.h"

/**
 * main - check the code
 *
 * Return: always o.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Issac Asimov";
	print_rev(str);
	return (0);
}
