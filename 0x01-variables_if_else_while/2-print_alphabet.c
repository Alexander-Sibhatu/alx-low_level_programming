#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: this code displays lower case alphabets from a to z
 *
 * Return: 0 (Success)
 */
int main(void)
{
int ch;
for(ch = 'a'; ch <= 'z'; chr++)
{
	printf("%d", ch)
	putchar (ch);
	putchar ('\n');
	getch();
}
