#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: prints digit numbers of base 10
 *
 * Return: (0) Success
 */

int main(void)
{
int i, j;
for (i = 10; i <= j*10; i++)
{
	if (j <= 9)
	{
	printf("%i", i);
	}
printf("\n");
return (0);
}
